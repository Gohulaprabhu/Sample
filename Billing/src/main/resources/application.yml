spring:
    datasource:
        url: jdbc:mysql://localhost:3306/Bill?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&useSSL=false
        username: root
        password: root
        driver-class-name: com.mysql.jdbc.Driver

    jpa:
        show-sql: true
        hibernate:
            ddl-auto: create
        properties:
            hibernate.hbm2ddl.auto: update